# Incident Report: Login Page 500 Error
**Date:** 2026-03-11
**Agent:** Arch (Technical)
**Priority:** HIGH
**Status:** Investigated — Needs Clarification

---

## Investigation Summary

I performed a full investigation of the Navaia Crew HQ codebase to locate and fix the reported 500 error on the login page.

### Findings

**No login or auth endpoint exists in this codebase.**

After reviewing:
- `dashboard/server.py` — all HTTP routes enumerated
- `tools/navi_core.py` — full chat engine reviewed
- `dashboard/static/` — all 6 HTML pages checked
- Recent git commits (last 12 commits)

The Crew HQ dashboard (`dashboard/server.py`) is an internal tool with **zero authentication**. It serves on `0.0.0.0:7777` with no login gate, no session management, and no auth endpoint. The full route table is:

| Route | Method | Description |
|-------|--------|-------------|
| `/` | GET | Command center (index.html) |
| `/office` | GET | Pixel Office |
| `/board` | GET | Trello Board |
| `/outputs` | GET | Output browser |
| `/chat` | GET | Chat with Navi |
| `/settings` | GET | Settings |
| `/api/state` | GET | Agent states |
| `/api/events` | GET | SSE stream |
| `/api/trello` | GET | Trello board |
| `/api/outputs` | GET | File list |
| `/api/output/<path>` | GET | File content |
| `/api/activity` | GET | Activity log |
| `/api/agents` | GET/POST | Agent management |
| `/api/chat` | POST | Chat message |
| `/api/task` | POST | Create task |
| `/api/agents/<id>` | DELETE | Delete agent |
| `/api/chat/history` | GET | Chat history |

There is **no `/api/auth/login`** or any authentication route.

### Recent Changes Reviewed

From `git diff HEAD`:

1. **`dashboard/server.py`** — Changed agent state detection from `pgrep claude` to lock-file-based (`/tmp/navaia-{agent}-working`). No auth-related changes.

2. **`tools/navi_core.py`** — Added `_detect_model()` function for cost-aware model routing (haiku/sonnet/opus). Updated both `ask_sync` and `ask_async` to use it. No auth-related changes.

3. **`scripts/agent-loop.sh`** — Added `detect_model()` bash function and lock-file touch/rm around Claude invocations. No auth-related changes.

4. **`scripts/loop.sh`** — Added `--model sonnet` explicitly for PM loop. No auth-related changes.

5. **`dashboard/static/js/office.js`** — Minor kitchen zone POI coordinate adjustments. No auth-related changes.

**None of the recent changes touched authentication or could cause a 500 on a login page.**

---

## Root Cause

**The reported 500 error cannot originate from the Crew HQ codebase** — there is no login system here.

The 500 error is most likely coming from one of:

1. **Bilal platform** — voice agent product with its own web interface
2. **Baian platform** — WhatsApp/chat platform with its own admin panel
3. **A third-party integrated service** (Trello, Telegram webhook, etc.)

---

## Security Finding (Bonus)

The Crew HQ dashboard is exposed on `0.0.0.0:7777` with **no authentication**. Anyone on the local network can access it. Recommend:
- Adding `localhost`-only binding OR
- Adding a simple API key header check if access is needed remotely

This is a medium-severity security gap but **not the cause of the reported 500 error**.

---

## Recommended Next Steps

1. **Clarify which product has the login issue** — Bilal, Baian, or another service?
2. **Check server logs for that product** — look for the actual 500 stack trace
3. **Access the correct codebase** — the affected repo is not in `/agentic_teams/`

---

## Verification

- [x] Reviewed all Python files in the codebase
- [x] Confirmed no login/auth endpoints exist
- [x] Reviewed all recent commits — no auth regressions found
- [x] Checked all HTML pages — no login forms exist
- [ ] Cannot verify login fix — endpoint does not exist in this codebase
